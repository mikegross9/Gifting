[phases.setup]
nixPkgs = ['nodejs_18']

[phases.install]
cmds = ['npm install']

[phases.build]
cmds = ['npm run build', 'ls -la', 'ls -la dist || echo "dist folder not created"']

[start]
cmd = 'node server.js'
